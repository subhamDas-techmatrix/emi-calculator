import {View, Text} from 'react-native';
import React from 'react';
import {TLoginProps} from '../utils/Types';

const Login = ({navigation}: TLoginProps) => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};

export default Login;
