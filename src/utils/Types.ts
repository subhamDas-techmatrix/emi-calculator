export type TLandingProps = {
    navigation: any,
}

export type TLoginProps = {
    navigation: any,
}

export type TNavButtonProps = {
    text: string,
    navigation: any,
    screen: string,
    btnStyle?: string,
    textStyle?: string,
}
